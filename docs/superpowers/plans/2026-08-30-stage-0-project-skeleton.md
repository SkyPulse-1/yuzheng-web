# 阶段 0：项目骨架执行计划

1. 校验本机 Node.js、npm、Git、Codex CLI 与 PyCharm 环境。
2. 使用 `create-next-app` 创建 `yuzheng-web`，启用 TypeScript、ESLint、Tailwind CSS、App Router 和 `src/`。
3. 运行静态检查与生产构建。
4. 启动本地开发服务器并检查首页响应。
5. 将架构约束与本阶段结果提交到本地 Git 仓库。

本阶段不创建业务页面、不连接 Supabase 数据、不调用火山知识库或 HiAgent、不执行部署。
